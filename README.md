# Excel-Lambda-Inliner-Optimizer
An inliner / optimiser that flattens custom user-written Excel LAMBDA formulas in Excel Labs "Advanced Formula Editor" modules into optimized LET-based formulas using ONLY stock Excel functions to eliminate abstraction overhead with non-stock Excel function calls.
