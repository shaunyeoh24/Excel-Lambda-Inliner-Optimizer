{
  "name": "excel-lambda-inliner-optimizer",
  "version": "1.0.0",
  "description": "An inliner / optimiser that flattens custom user-written Excel LAMBDA formulas in Excel Labs \"Advanced Formula Editor\" modules into optimized LET-based formulas using ONLY stock Excel functions to eliminate abstraction overhead with non-stock Excel function calls.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^24.10.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
